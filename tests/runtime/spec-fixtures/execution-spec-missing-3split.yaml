schema_kind: execution_spec
schema_version: 1
schema_dialect: seeyue_workflow_schema/v1
status: draft
node_action_schema:
  action_types:
    - read
    - write
  inputs:
    source_enum:
      - runtime_state
  outputs:
    destination_enum:
      - runtime_state
# execute_3split is intentionally missing — expect ERROR from validator
persona_action_constraints:
  author:
    may_write: true
    may_run: true
